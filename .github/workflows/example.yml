name: example workflow

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@v3
      name: example step
      run: echo "Hello World"
